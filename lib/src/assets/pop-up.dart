import 'package:flutter/material.dart';

import '../colors/colors.dart';
/*
class PopUpsError extends StatefulWidget {

  @override
  State<PopUpsError> createState() => _PopUpsErrorState();
}

class _PopUpsErrorState extends State<PopUpsError> {
  final _formKey = GlobalKey<FormState>();
  @override
  Widget build(BuildContext context) {
    return showDialog(
      context: context,
      builder: (BuildContext context) {
        return AlertDialog(
          title: Text('Título del Popup'),
          content: Text('Contenido del Popup'),
          actions: <Widget>[
            // Botón de cerrar
            TextButton(
              onPressed: () {
                Navigator.of(context).pop();
              },
              child: Text('Cerrar'),
            ),
          ],
        );
      },
    );
  }
}
*/